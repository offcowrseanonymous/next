name: Next
options:
  bundleIdPrefix: com.local.next
  deploymentTarget:
    iOS: "15.0"
targets:
  Next:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - Next
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.local.next.app
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"
